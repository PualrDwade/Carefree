<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.csu.carefree.Persistence.SigonMapper">
    <cache/>
    <!--
    //通过用户名得到登陆账号信息
    Sigon getSigon(String username);

    //用过注册账号,
    boolean setSigon(String username, String password);

    //通过找回密码
    boolean updateSigon(String username, String password);
        通过省份ID查询省份信息
    -->

    <!--通过用户名得到账户信息-->
    <select id="getSigon" parameterType="java.lang.String" resultType="com.csu.carefree.Model.Account.Sigon">
        select * from Account_signon where username=#{username}
    </select>

</mapper>